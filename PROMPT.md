# 项目执行提示词（PROMPT）

> 用途：本项目所有工作轮次的自我指令。开工前先读本文件与 `agent-security-v2.md`。若两者冲突，以本文件为准；若本文件与实测结果冲突，以实测结果为准并回来修订本文件。

## 1. 项目是什么

把 `/root/ai监督/agent-security-v2.md` 提出的架构（授权沿委托链向下衰减、证据沿委托链逆向上行）从一份设计说明，做成**可运行、可验证、有实测数据**的完整研究工件。

不是写更多文档，而是**把断言降落到可执行代码与可复现实验上**。

## 2. 交付物

| 编号 | 交付物 | 路径 | 完成判据 |
|---|---|---|---|
| D1 | 项目提示词 | `PROMPT.md` | 本文件 |
| D2 | 形式化规格 | `spec/` | 数据结构、协议、算法、状态机、不变式齐全；每条安全性质给出"由哪段机制保证" |
| D3 | 参考实现 | `src/` | `python3 -m src.demo` 能跑通完整链路；真实 Ed25519 签名，不做假的 |
| D4 | 攻击与基线 | `experiments/` | B0/B1/B2/P 四种配置均可运行；七类攻击可复现 |
| D5 | 实测结果 | `results/` | 由脚本生成，含原始 JSON 与汇总表；每个数字可回溯到某次真实运行 |
| D6 | 论文初稿 | `paper/draft.md` | 含威胁模型、机制、性质、实验、局限；数字来自 D5，不得手填 |
| D7 | 导航 | `README.md` | 复现步骤一条命令；结论清单 |

## 3. 铁律（违反即返工）

1. **没有真实输出就不许写"已验证"。** 任何通过/指标必须来自本会话实际执行并保留在 `results/` 的输出。
2. **论文里的每个数字必须是脚本产物**，不得估算、不得从别处抄。
3. **不得夸大。** 可证明的性质标"定理"，启发式的标"经验"，未测的标"未验证"。P5（传播上界）是定理，P5'（实际半径小）是经验命题，两者永远分开写。
4. **不得声称赢过中心 PDP 的安全性。** 本方案对 B1 的正当优势只有：单点可用性、N² 吞吐/延迟、分区可用。若实验显示相反，照实写。
5. **代替物必须标注。** 若用确定性统计器代替 LLM 漂移检测器，必须在代码注释、结果表和论文里三处写明"这是代理实现，非 LLM"。
6. **发现反例不得静默丢弃**，要么改结论，要么写进"局限"。

## 4. 技术约束

- Python 3.14，可用 `cryptography`(46.0.7) / `pynacl` / `numpy` / `pandas`。
- **本机网络可用**（IPv4；IPv6 路由被拒，curl 会因先试 IPv6 而超时——用 Python/`-4` 规避）。
- **无 LLM 凭证可用**：环境变量中没有任何 key；`/root/.dsh/.credentials.yaml` 存在但属 harness 自身，不用于本项目实验。
- 默认 ed25519（`cryptography.hazmat.primitives.asymmetric.ed25519`）。
- 纯标准库优先；除上述已装库外不引新依赖。
- 实验必须**确定性可复现**：所有随机性走固定种子，日志记录种子。
- 单次全量实验目标 < 5 分钟。

## 5. 分段计划

- **阶段 1（D2）**：形式化规格。先定对象模型与令牌代数，再定协议消息，最后把 v2 的 P1–P5 逐条映射到机制。
- **阶段 2（D3）**：实现。顺序：crypto → authority/epoch → token 衰减 → PEP → receipts → revocation/gossip → drift plane → 世界模型与 Agent 进程。
- **阶段 3（D4）**：基线 B0/B1/B2 与攻击集（身份投毒、越权、重放、回滚、split-view、串通 k、链式传播）。
- **阶段 4（D5）**：跑实验，出 JSON + 汇总表 + 延迟/成本曲线。
- **阶段 5（D6/D7）**：论文初稿与导航，回填真实数字。

每阶段结束：跑一次自检脚本，把结论写进 `results/`，然后才进下一阶段。

## 6. 完成判据（全部满足才算完成）

- [x] `python3 -m src.demo` 退出码 0，打印完整链路
- [x] `python3 -m experiments.run_all` 产出全部结果文件，退出码 0
- [x] 攻击类别 A1–A5 各有 P/B1/B2/B0 四模式对照数据；A6 split-view、A7 收据伪造、A8 检测器注入
      为机制级验证（与模式无关）
- [x] `python3 -m experiments.selfcheck`（44 项不变式与性质断言）退出码 0
- [x] `python3 -m experiments.verify_claims` 核对文档引用数字与 `results.json` 一致，退出码 0
- [x] 传播半径、撤销延迟、延迟增量、过近似率四项指标有实测值
- [x] 论文初稿中不存在无出处的数字
- [x] `python3 -m tools.audit_register`（锚定登记表三条件）退出码 0
- [x] `python3 -m tools.verify_fixes`（全部〔已报告过的修复〕回查）退出码 0
- [x] README 的复现命令实测可跑（删除 `results/` 与 `paper/results_appendix.md` 后全量重跑通过）

## 6.1 开工条件（外部审读后修订）

**工件的稳定性不由「声明是否稳定」判定**——六轮审视每轮都在缩小声明，未收敛。
开工条件是**反馈回路齐全**，即可判定的三条：

1. 每个输入有外部锚定，或显式标注为自造并给出处置
2. 每个基线有独立实现路径，或显式标注为共用路径
3. 每条断言有机械检查，或显式标注未实现/未验证

由 `data/anchoring_register.json` 登记、`tools/audit_register.py` 校验。
**该工具只保证没有未标注项，不判断标注是否诚实。**

## 6.2 报告纪律（外部审读与复现后新增）

**凡向用户报告"已修复/已更正"，必须能在 `tools/verify_fixes.py` 中被机械回查。**

理由：本项目实际发生过〔报告已修复、替换静默失败、未验证〕——`str.replace` 的目标与文件内容
不匹配时什么都不做且不报错。**静默失败防不住，只能靠检查**。同一教训已出现三次：
模式包含的随机性质测试、粗化单调性检查、规格↔代码字段一致性检查，**每次都是刚加上就抓到我自己的错**。

补丁纪律：任何字符串替换**必须**先 `assert 目标 in 内容`，替换后再读回确认。

## 7. 反模式（明确不要做）

- 不得新增**未在登记表中标注**的输入、基线或断言——校验器会失败。

- 不要用 workflow / ralph 扇出，除非用户明确要求。
- 不要为了让曲线好看而调参后再隐藏调参；调参必须记录在 `results/config.json`。
- 不要写"未来工作将……"来代替本可完成的部分。能做的现在做，做不了的写进局限。
- 不要把 `/root/dsh-test/package` 与本项目混淆，那是 DSH 自身实现，与本项目无关。
