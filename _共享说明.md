# 这个共享目录里是什么

从客机 `/root/ai监督` 拷出来的完整项目副本，外加文章与全部截图。

## 目录

| 路径 | 内容 |
|---|---|
| `article/draft.md` | 技术文章正文（约 1.5 万字） |
| `article/shots/*.png` | **六张截图**，其中 `s1` 是演示性破坏（图注已标明） |
| `article/shots/CAPTIONS.md` | 图注与各自在文中位置的清单 |
| `README.md` | 项目总说明与全部实测结论 |
| `THIRD_PARTY.md` | 第三方署名与 MIT 许可证正文 |
| `spec/spec.md` | 架构规格（含装置自证禁令） |
| `src/` `experiments/` `tools/` | 实现、实验、审计工具 |
| `results/` | 全部实验产物（`summary.md` 可读） |
| `paper/` | 审读记录、自我修正日志、声明→证据对照表 |
| `replication/` | 独立复现与差分测试 |

## 主动没有拷的东西

| 未拷 | 为什么 |
|---|---|
| `.secrets/qwen_api_key` | **API 密钥。共享目录可能被同步或挂载到别处，不把凭证放进共享盘。** 已扫描确认副本内无任何密钥残留。 |
| `.cache/` | pip 安装的 agentdojo 包缓存（1.6M），可由依赖重装，无保留价值。 |
| 若干 `.e32.log` `.linetest.out` 之类别名临时日志 | 中间产物。 |

## 想在这台机器上重新跑

需要在客机（不是宿主机）上执行，且要有 root 权限，因为项目在 `/root` 下：

```bash
sudo sh -c 'cd /root/ai监督 && python3 -m experiments.verify_claims'
```

七条管线命令：

```bash
sudo sh -c 'cd /root/ai监督 && python3 -m src.demo'
sudo sh -c 'cd /root/ai监督 && python3 -m experiments.run_all'
sudo sh -c 'cd /root/ai监督 && python3 -m experiments.selfcheck'
sudo sh -c 'cd /root/ai监督 && python3 -m experiments.verify_claims'
sudo sh -c 'cd /root/ai监督 && python3 -m experiments.topology_tests'
sudo sh -c 'cd /root/ai监督 && python3 -m tools.audit_register'
sudo sh -c 'cd /root/ai监督 && python3 -m tools.verify_fixes'
```

重新生成截图：

```bash
sudo sh -c 'cd /root/ai监督 && python3 -m tools.make_shots'
```

依赖：`cryptography`（Ed25519）、`PIL`、`chromium`（截图用）。除 E32 与跨家族审读外，其余实验**不需要联网**。
