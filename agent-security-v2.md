# 委托约束与证据回流：多 Agent 运行时安全架构（v2）

> v2 是 v1 的重写，不是补丁。保留的问题意识（单点被攻破不应成为他人的可信事实）、保留委托关系作为核心结构，其余机制层重建。

---

## 0. 相对 v1 的改动清单

| v1 | v2 | 原因 |
|---|---|---|
| Agent 自己持有私钥并签名 | 密钥与执行权在 Agent 进程外的 PEP | 被注入的 Agent 用自己的合法私钥签任何东西，密码学层对威胁模型零防护 |
| 接收方 B 做一致性检测 | B 只做范围强制 + 签收据；漂移检测移到旁路平面 | 检测器不能跑在被攻击者能直接说话的同一条链路上 |
| 检测基准来自 A 的委托描述 | 范围来自权威签发 + 逐级衰减的凭证，密码学保证 | v1 会被"T0 写一个宽松 scope"整体绕过（基线中毒） |
| Identity / Credential / Key 三版本 + 各 Agent HighestKnownVersion | 单调 epoch + 短 TTL + 收据 | 三版本冗余，真正需要的只是单调性与有界陈旧 |
| "委托产生监督"（B 有监督 A 的责任） | "授权向下衰减，证据向上回流" | 监督是职责声明，无法强制；衰减与归因是机制，可证明 |
| Level 3 一律人工仲裁 | 分级：令牌级处置 → 边级 → 主体级 | 人工队列会被长尾误报淹没，方案先死于可用性 |
| 声称三条 containment 性质 | 两条可证明，一条降级为经验命题 | 局部动作给不出图级保证 |

v1 的核心直觉是对的：**控制点应当放在委托的接收端，而不是中心**。v2 的工作是把这条直觉变成可验证的机制，并诚实标出它换不来什么。

---

## 1. 重新定位

两条支柱，必须同时成立才算方案成立。

**支柱 A（工程）**：把授权判定下沉到每条委托边的接收端，用衰减式凭证替代中心策略判定点（PDP），在持平的安全保证下换取延迟、可用性和可扩展性；代价是撤销从"即时"退化为"有界延迟"。

**支柱 B（研究）**：自然语言子目标到机器可检查范围的**语义衰减**问题——如何从 NL 子任务推出 scope，以及这种推导的过近似（over-approximation）有多少、代价是什么。

核心命题（替换 v1 的 "Delegation creates Oversight"）：

> **授权沿委托链向下逐级衰减，证据沿委托链逆向上行。安全性来自"下行的衰减"与"上行的归因"，不来自任何 Agent 被指派去监督另一个 Agent。**

"反向"体现在：**强制点在委托的接收方，证据从接收方回流到发起的上游**。方向的倒置是这套方案真正想说的东西。

### 必须正面回答的致命质疑

> 中心 gatekeeper 能看到全部请求，也能做漂移检测，而且更准。为什么不直接用它？

诚实答案只有三条：**单点可用性、N² 消息量下的延迟与吞吐、弱网/分区下不可用的能力**。如果这三条在你的目标场景里不重要，就不要做这套东西——中心 PDP 更强，也更好维护。方案不得声称"比中心方案检测更准"。

---

## 2. 威胁模型

**攻击者可以**：通过 prompt injection 完全控制任意 Agent 的进程；伪造 Agent 的一切文本输出；重放截获的消息；诱导 Agent 提出任意越权请求；在若干 Agent 之间串通（上限 k 个）。

**攻击者不可以**：读改 Agent 进程外的 PEP/密钥；伪造权威或 PEP 的签名；读写旁路日志平面。

**明确不设防（写进论文的 out of scope）**：攻击者进入 TCB（PEP、权威私钥、日志平面）；时序/功耗侧信道；纯 DoS；模型层的欺骗性推理但只要最终动作在授权范围内（这类只能靠旁路统计发现）。

由此得出：**被攻破的 Agent 能做的事，上界等于它合法持有的全部令牌范围的并集。**安全目标不是"检测异常行为并阻止"，而是"让攻击者只能待在它本来就有的权限盒子里"。检测是附加价值，不是安全边界。

---

## 3. 信任锚：进程外 PEP

```
Agent 进程（不可信）        PEP / sidecar（TCB）           权威（TCB）
   ├ 提议 action  ──────────► ├ 验令牌链
   ├ 持有会话            │      ├ 验范围 ⊇ action
   ├ 无密钥              │      ├ 验 epoch / TTL / 撤销
   └ 无副作用能力        │      ├ 签收据 → 日志平面
                         │      └ 执行副作用
```

- Agent 只产出**提议**（结构化 action + 所需 capability），永远不接触私钥、不直接产生副作用。
- 被完全注入的 Agent 能做的只剩"不断提议"，PEP 逐条拒绝。
- 这是 v1 缺失的那一层。没有它，v1 的全部密码学设计不成立；有它，v1 里一大半复杂度（局部身份视图、自证轮换）直接消失。

---

## 4. 凭证模型（替换 v1 的三版本制）

v1 的 Identity/Credential/Key 三版本、逐 Agent HighestKnownVersion，实际需求只有两条：**单调性**（防回滚）与**有界陈旧**（防撤销延迟）。压缩为：

```
Subject = {
  subject_id           # 稳定标识
  role_attested[]      # 权威背书，非自我声明
  key_pub, key_epoch   # 单调递增
  cred_epoch           # 单调递增，权威签名
  ttl                  # 短有效期
}
```

- **回滚防护**：验证方缓存 `max_seen_epoch`，`epoch < max_seen` 直接拒。
- **撤销延迟**：由 TTL 上界保证 —— 撤销后最长 `TTL + 时钟偏移` 内全网不可用。这就是"局部视图不收敛"的代价，写清楚，然后测它。
- **Split-view 攻击**（v1 未处理）：权威对 B 发 v8、对 C 发 v7，两边都合法，永久分叉 → 误报风暴。对策二选一：(a) 权威发布 append-only 的 Merkle 日志头，任意两个 Agent 交换日志头即可检出等价分歧（CT 式）；(b) 承认信任单一一致点。**不要留白**，这直接决定架构选型。
- **轮换**：计划轮换由新旧密钥双签；**密钥泄露场景下不得由旧密钥自证**（v1 第八节的漏洞：攻击者可抢先用自己的新公钥完成一次"合法轮换"），必须由权威单方撤销 + 重签。

---

## 5. 委托即契约：衰减式令牌

A 委托 B 时，A 在本地（离线，不经过权威）从自己的根令牌派生出子令牌：

```
Token_child = {
  root_id, issuer_chain = [Authority → A → ... → A],
  subject = B,
  scope ⊆ scope(A),                    # 密码学保证的衰减
  task_id, not_after, parent_task,
  sigs[]                               # 每级一个签名，公钥可离线验
}
```

性质：

- **离线可验**：B 用公钥链 + 权威根公钥本地验证，不查中心。
- **不能越权授予**：`scope_child ⊆ scope(A)` 由凭证代数保证，A 无论被怎么注入都**无法**给出超出自身权限的委托。这直接消灭 v1 的基线中毒——篡改 T0 的委托描述无用，因为范围不是描述，是签名结构。
- 与 Macaroon / Biscuit / ZCAP-LD 一类衰减凭证同源，论文必须引用并说明差异（见 §11）。

工程注记：链长随委托深度线性增长，验证成本 O(depth) 次签名验算，消息体积同阶。深度上限或签名聚合是必须做的工程取舍，v1 完全没提。

---

## 6. 接收端强制：执行点 + 见证人

B 收到请求时的动作：

1. 验 A 的令牌链（离线）。
2. 验请求的 required capability ⊆ token.scope，且 `task_id` 匹配、未过期。
3. **通过则执行并签收据；不通过则拒绝并签收据。**
4. 收据（receipt）流向日志平面：

```
Receipt = {
  edge: A→B, task_id, token_id,
  requested_capability, target_resource,
  decision: allow|deny, reason_code,
  ts, sig_B
}
```

关键点：收据由 **B 签名**，内容是"我收到了什么、我怎么判的"。攻击者可以控制请求内容，但无法伪造 B 的签名——所以**收据是 A 实际行为的地面真值**，而且是结构化字段，不是自由文本。

这就是"反向监督"的可实现版本：B 不是监督者，B 是**该边的执行点与该边事实的见证人**。它不需要理解 A 为什么这么做，也不需要判断 A 的意图。

B 不做的（v1 让它做、它做不到的）：观察 A 对其他 Agent 的行为（看不见）、评估 A 的整体行为基线（无观测面）、判断 A 的角色声明是否漂移（角色根本不该由声明决定）。

---

## 7. 旁路：证据平面与漂移检测

漂移检测读的是**收据流**，不是 prompt 文本。

```
Receipts ──► 证据平面（离线/近线）
                ├ 行为图（谁在何时对谁做了什么）
                ├ 漂移评分：与同角色历史、与任务语义的偏离
                ├ 串通检测：k 个 Agent 之间的异常闭环
                └ 输出：撤哪个令牌 / 哪条边 / 是否需要换主体密钥
```

为什么这解决了 v1 的第二致命问题：检测器处理的是结构化字段（capability、resource、decision、task），**不被 A 的文本内容诱导**。A 即使写满诱导性语言，收据里记录的仍是"它请求了 `db.export` 且被拒"。

这层是**启发式**，不是安全边界。它带来的是检测速度与归因能力，不是保证。混进安全性质里是 v1 最大的表述错误。

---

## 8. 分级处置（替换"Level 3 一律人工"）

| 级别 | 触发 | 动作 | 半径 |
|---|---|---|---|
| 0 | 拒绝且符合预期 | 记录 | — |
| 1 | 范围外请求、单次 | 拒绝 + 收据上报 | 边 |
| 2 | 同边反复、或漂移评分超阈 | 缩短该边令牌 TTL / 收窄 scope / 只读降级 | 边 |
| 3 | 跨边模式、疑似主体失陷 | 撤销该主体全部未过期令牌 + 沙箱重放 | 主体 |
| 4 | 疑似权威/PEP 失陷、串通闭环 | 冻结相关边 + 人工 | 子系统 |

处置对象优先级：**令牌 > 边 > 主体**。撤销令牌半径最小、代价最低、不需要人工。v1 直接跳到"暂停 Agent 并人工复核"，是把最贵的动作放在最前面。

Agent 本身在任何级别都没有"暂停另一个 Agent"的权限，只有 PEP 和令牌平面能改状态。

---

## 9. 安全性质

**可证明（在 §2 假设下）**

- **P1 授权包含**：不存在执行的动作超出其令牌链交集。由 §5 的衰减结构与 §6 的强制点保证。
- **P2 回滚抵抗**：`epoch < max_seen` 一律拒绝，重放旧凭证无效。
- **P3 撤销有界**：撤销后最长 `TTL + skew` 内不可接受。
- **P4 归因**：每个被执行的副作用都有一条以接收方签名为终点、可追溯到根令牌的收据链。
- **P5 传播上界（这条其实是定理）**：被攻破主体可达的动作集合 ⊆ 其合法持有的令牌范围之并的传递闭包。

**经验命题（不得写成定理）**

- **P5'**：在真实任务拓扑下，这个并集通常很小，因而实际横向半径小。这需要实测，取决于 scope 的过近似程度，而后者取决于 §1 支柱 B。
- 漂移检测的 Precision/Recall，以及在正常长尾上的 FPR。

v1 把 P5 写成"理想情况下传播自动停止"，方向对，但把它当作机制结论而非测量对象，是表述越界。

---

## 10. 残余风险与真正的开放问题

1. **攻击面从"信任传播"转移到"范围表达能力"。** 令牌再严，也严不过 scope 定义。NL 子任务 → capability 集合的映射若过宽，攻击者就在这个宽度里活动。**这就是支柱 B 的入口，也是本方案唯一真正的研究缺口**：
   - 过近似率如何定义与测量？（授权宽度 / 最小必要宽度）
   - 能否自动从子目标推导 scope，代价是多少可用性？
   - 过近似与误报的帕累托前沿在哪？
2. **失陷的 B 可以在其令牌范围内滥用**，且完整失陷时其收据不可信。缓解靠范围 + TTL + 交叉收据（同一动作在上下游各留一份），不能消除。
3. **串通 k 个 Agent** 时，衰减链无法阻止它们在各自范围内拼出一个大范围。这是唯一的真实传播路径，必须专门设计实验。
4. **TTL 与延迟的取舍**：TTL 越短越安全越贵。需要给出延迟-安全曲线，而不是拍一个值。

---

## 11. 与已有工作的区分（必须做，否则被判为组合）

| 已有 | 关系 |
|---|---|
| Macaroon / Biscuit / ZCAP-LD | 衰减式凭证的成熟实现，v2 §5 直接站在其上。**不是新东西，必须引用** |
| SPKI/SDSI、web-of-trust | 局部命名与本地信任视图的前身；v2 有意放弃"局部身份视图"，理由见 §0 |
| OAuth 短令牌、CT 日志 | 有界撤销延迟与等价分歧检测的标准手段 |
| Supervisor / critic agent、双 LLM 模式 | v1 的"B 监督 A"与之高度重合，且缺乏可强制性与可观测性；v2 已放弃该表述 |

**可辩护的差异**：把"委托范围"从自然语言意图落到可离线验证的凭证结构，并在 LLM Agent 的委托拓扑上测量两件事——横向传播半径的收缩幅度，以及语义衰减的过近似代价。这是一个评估型工作，不是一个新机制。以评估型工作投稿比以机制型投稿安全得多。

---

## 12. 实验设计

**对照必须公平**（v1 拿最简单的中心方案当基线是稻草人）：

- B0 无强制（单信任域）
- B1 **中心 PDP**，每条跨 Agent 调用都过一次中心判定 —— 这是真正的对手，不是陪衬
- B2 Prompt 层护栏/filter
- P 本方案（边级 PEP + 衰减令牌 + 收据）

**若 P 在安全指标上赢不了 B1，就只能靠延迟/可用性/分区能力赢，指标必须按这个来设计**，否则实验做出来是自己打自己。

**测什么**

- 安全：传播半径（跳数与受影响 Agent 数）、越权成功率、撤销延迟分布、回滚重放成功率、k 串通下的半径
- 代价：p50/p99 端到端延迟增量、令牌链长对体积/验签成本的放大、token 消耗、吞吐上限对比 B1 的瓶颈点
- 质量：过近似率（授权的 capability 数 / 最小必要数）、漂移检测的 Precision/Recall 与长尾 FPR、每千任务的人工升级次数
- 鲁棒：把 k 个 Agent 全部注入，看半径是否仍被令牌约束住（这是 P1/P5 的直接检验）

**场景与攻击**：用公开的多 Agent 任务基准与公开的注入攻击集，不要自造攻击集——自造的攻击集会被质疑为"针对自己方案设计"。

---

## 13. 自检清单：以下任一条答不上来，就砍掉对应部分

1. 密钥和执行权是否真的在 Agent 进程之外？（否则 §4–§9 全部失效）
2. 漂移检测器是否只读结构化收据、不读不可信文本？（否则检测器可被注入）
3. 委托范围是否由签名结构保证，而非 A 的描述？（否则基线中毒）
4. 撤销延迟是否被明确界定量化，并作为指标测量？（否则"局部视图"这个卖点不成立）
5. Split-view 是用日志头检出，还是明确承认中心一致点？（必须二选一，不能留白）
6. 你打算靠什么赢过中心 PDP？答不出就说明这套东西不该做。
